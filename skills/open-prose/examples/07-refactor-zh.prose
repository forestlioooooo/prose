# 重构代码
# 系统化重构工作流

# 第一步：评估当前状态
# 分析目标代码并识别代码坏味道
session "分析目标代码并识别代码坏味道：重复、函数过长、命名不清晰、紧耦合。"

# 第二步：规划重构
# 创建重构计划，按优先级列出具体更改
session "创建一个重构计划。按优先级列出具体更改，从最安全的更改开始。"

# 第三步：确保测试覆盖率
# 检查待重构代码的测试覆盖率
session "检查待重构代码的测试覆盖率。在更改前添加任何缺失的测试。"

# 第四步：执行重构
# 实施计划中的第一个重构
session "实施你计划中的第一个重构。进行一个单一的重点更改。"

# 第五步：验证行为
# 运行测试验证重构保持了原有行为
session "运行测试以验证重构保持了原有行为。如果测试失败，调查并修复。"

# 第六步：文档化更改
# 更新受重构影响的文档
session "更新受重构影响的任何文档。添加注释解释非明显的设计决策。"
